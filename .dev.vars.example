# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Cookie encryption key (generate a secure random string)
COOKIE_ENCRYPTION_KEY=your-secure-encryption-key

# Optional: Comma-separated list of allowed emails for access control
# ALLOWED_EMAILS=user1@example.com,user2@example.com

# Optional: Restrict logins to a specific Google Workspace domain
# HOSTED_DOMAIN=example.com

# Optional: Override the Worker URL for image hosting (defaults to workers.dev domain)
# For local development, uncomment and use:
# WORKER_URL=http://localhost:8787
# For production, use your workers.dev domain:
# WORKER_URL=https://image-generation-mcp.john-welsh.workers.dev